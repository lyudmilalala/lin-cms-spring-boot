<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.talelin.latticy.mapper.BookMapper">

    <resultMap id="BaseResultMap" type="io.github.talelin.latticy.model.BookDO">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="title" jdbcType="VARCHAR" property="title"/>
        <result column="author" jdbcType="INTEGER" property="author"/>
        <result column="content" jdbcType="LONGVARCHAR" property="content"/>
        <result column="category" jdbcType="VARCHAR" property="category"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
        <result column="delete_time" jdbcType="TIMESTAMP" property="deleteTime"/>
    </resultMap>

    <!--CONCAT("%",#{q},"%")-->
    <select id="selectActiveByTitleLike" resultMap="BaseResultMap">
        SELECT * FROM project p WHERE p.title LIKE %#{searchContent}% AND p.delete_time IS NULL
    </select>

    <select id="selectActiveByCategory" resultMap="BaseResultMap">
        SELECT * FROM project p WHERE p.category=#{category} AND p.delete_time IS NULL
    </select>

    <select id="selectActiveByCategoryAndTitleLike" resultMap="BaseResultMap">
        SELECT * FROM project p WHERE p.category=#{category} AND
        p.title LIKE %#{searchContent}% AND p.delete_time IS NULL
    </select>

    <select id="selectActivePageByTitleLike" resultMap="BaseResultMap">
        SELECT * FROM project p WHERE p.title LIKE %#{searchContent}% AND
        p.delete_time IS NULL LIMIT #{pageStartIdx},#{pageSizes}
    </select>

    <select id="selectActivePageByCategory" resultMap="BaseResultMap">
        SELECT * FROM project p WHERE p.category=#{category} AND
        p.delete_time IS NULL LIMIT #{pageStartIdx},#{pageSizes}
    </select>

    <select id="selectActivePageByCategoryAndTitleLike" resultMap="BaseResultMap">
        SELECT * FROM project p WHERE p.category=#{category} AND
        p.title LIKE %#{searchContent}% AND p.delete_time IS NULL
        LIMIT #{pageStartIdx},#{pageSizes}
    </select>
</mapper>